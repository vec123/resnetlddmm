"""Vendored subset of Meta's fairchem EquiformerV3 (experimental) block subsystem.

Source: Theory/equiformer_v3/experimental/models/equiformer_v3/ (MIT). Only the block
subsystem is copied here (no fairchem dependency); the fairchem-coupled full-model
files (equiformer_v3.py / _dens.py, which do energy/force/stress prediction) are
intentionally omitted. Used by ``..equiformer.EquiformerTransformer``.
"""
